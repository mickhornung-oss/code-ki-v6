# CI requirements — excludes llama-cpp-python (C extension, not needed for tests)
# Tests use _FakeRuntime instead of the real model runtime.
fastapi==0.135.2
uvicorn==0.42.0
pydantic==2.12.5
pytest==7.4.3
pytest-cov==4.1.0
black==23.12.1
isort==5.13.2
flake8==6.1.0
